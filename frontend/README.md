# SchemeAssist Frontend

Run the frontend in development (Vite) and proxy `/api` to the backend running on `http://127.0.0.1:8000`.

Install dependencies:

See the top-level `README.md` for project setup, frontend quickstart and testing instructions.

Quick frontend steps (short):

```bash
cd frontend
npm install
npm run dev
```
